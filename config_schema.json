{
  "fields": [
    {
      "name": "api-token",
      "displayName": "API Token",
      "description": "Okta API token",
      "isRequired": true,
      "isSecret": true,
      "stringField": {
        "rules": {
          "isRequired": true
        }
      }
    },
    {
      "name": "domain",
      "displayName": "Domain",
      "description": "Okta domain (e.g., company.okta.com)",
      "isRequired": true,
      "stringField": {
        "rules": {
          "isRequired": true
        }
      }
    },
    {
      "name": "email-domains",
      "displayName": "Email Domains",
      "description": "Comma-separated list of email domains to filter users (e.g., example.com,company.com)",
      "isRequired": true,
      "stringSliceField": {
        "rules": {
          "isRequired": true
        }
      }
    },
    {
      "name": "group-name-filter",
      "displayName": "Group Name Filter",
      "description": "String to filter group names (only groups containing this string will be synced)",
      "stringField": {
        "rules": {}
      }
    },
    {
      "name": "log-level",
      "description": "The log level: debug, info, warn, error",
      "isOps": true,
      "stringField": {
        "defaultValue": "info"
      }
    },
    {
      "name": "log-level-debug-expires-at",
      "description": "The timestamp indicating when debug-level logging should expire",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint",
      "description": "The endpoint of the OpenTelemetry collector to send observability data to (used for both tracing and logging if specific endpoints are not provided)",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-cert",
      "description": "A PEM-encoded certificate to use as a CA for TLS connections to the OpenTelemetry collector",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-cert-path",
      "description": "Path to a file containing a PEM-encoded certificate to use as a CA for TLS connections to the OpenTelemetry collector",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-insecure",
      "description": "Allow insecure connections to the OpenTelemetry collector",
      "isOps": true,
      "boolField": {}
    },
    {
      "name": "otel-logging-disabled",
      "description": "Disable OpenTelemetry logging",
      "isOps": true,
      "boolField": {}
    },
    {
      "name": "otel-tracing-disabled",
      "description": "Disable OpenTelemetry tracing",
      "isOps": true,
      "boolField": {}
    }
  ],
  "constraints": [
    {
      "kind": "CONSTRAINT_KIND_REQUIRED_TOGETHER",
      "fieldNames": [
        "domain",
        "api-token"
      ]
    }
  ],
  "displayName": "Okta CIAM Workforce",
  "helpUrl": "/docs/baton/okta-ciam-workforce",
  "iconUrl": "/static/app-icons/okta.svg"
}